<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.mapper.ProductMapper">
	<insert id="productInsert" parameterType="Product">
      <selectKey keyProperty="pnum" resultType="_int" order="BEFORE">
         select product_seq.nextval from dual
      </selectKey>
      insert into product values(#{pnum},#{pname},#{price},#{saleprice},
      #{pcompany},#{pcontents},#{pspec},#{point},sysdate,
      'noimage.png','noimage.png','noimage.png',#{upCg_code},#{downCg_code}) 
   </insert>
</mapper>